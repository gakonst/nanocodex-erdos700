name: Lean proof

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - name: Set up Lean and build
        uses: leanprover/lean-action@v1
        with:
          lake-package-directory: proof
          build: true
          test: false
          lint: false
      - name: Verify theorem dependencies
        run: ERDOS700_SKIP_BUILD=1 ./proof/scripts/verify.sh
