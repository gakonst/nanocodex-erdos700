# Erdős 700(iii): adaptive first-layer factorial moments

Attack only UHFL(A), as stated in
`runs/erdos700-iii-upper-half-first-layer-frontier-20260725/report.md`.
Never inspect `events.jsonl`, snapshots, credentials, or telemetry.

Fix A>1, m=ceil(A), and the upper-half residual owner set U. The goal is one
legal integer k for which at least m distinct p_i in U divide D_n(k). Use the
exact right-closed block formula from the frontier report. Preserve one common
index and positive Kummer losses.

The co-divisor LAMI route is dead: recover
`runs/outer-loop-1784948761-3803975/round-1-synthesis.md` and
`runs/math-1784994375-4111087/candidate.md`. Literal component-product rows
can have support at most one on an unconditional hard family. Therefore build
an adaptive multiplier distribution, not a new law on the same rows.

Seek a rigorous positive m-th falling-factorial moment for the exact
first-layer count

    Z(k) = #{i in U : p_i | D_n(k)}

over an explicitly constructed finite ensemble of literal rows. Candidate
ensembles include cofactor power-moment multipliers, signed/reflected prefix
rows, and multiplier families selected after the actual digit phases are
known. A bound E[(Z)_m]>0 is sufficient only if every sample is one admissible
row in K_n.

Required workers:
- exact one-prime and joint m-prime incidence formulae;
- a phase-sensitive moment lower bound;
- adaptive multiplier/conditional-expectation construction;
- upper-half/dyadic mass transfer;
- counterexample designer controlling every multiplier in the ensemble;
- independent quantifier and endpoint referee.

Do not multiply marginal densities, use abstract hypergraph cover, or infer a
common row from separate witnesses. Use at most two focused exact falsifiers.
Return a full UHFL proof, an unconditional unbounded all-k counterfamily, or
the weakest exact moment lemma that remains both unrefuted and sufficient.
