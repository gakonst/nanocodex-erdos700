# Part (iii) local-session coverage audit

Date: 2026-07-27

This is the local-history companion to
`docs/part-iii-exploration-map.md` and
`docs/part-iii-run-coverage-audit.md`. The run audit inventories persisted
dev-box campaign directories. This file inventories the claim-bearing Codex
session tree, including ideas that were discussed locally but did not become
their own dev-box run directory.

## Root sessions

| Session | Size / role | Disposition |
|---|---|---|
| `019f8bde-ae0f-77f0-a4e4-4d3e84f5381c` | 79 MB, 29,215 lines; original 22 July research and orchestration thread | Read for user decisions, worker summaries, launch prompts, claimed results, corrections, and terminal status. No target solution was found. |
| `019f9f8c-215d-7ff0-a390-f79351c726d0` | 34 MB, 10,489 lines; 26–27 July Part-(iii) continuation | Read together with all 28 mathematical descendants listed below. No target solution was found. |

The first root had three Part-(iii)-relevant specialist forks:

| Agent path | Disposition |
|---|---|
| `/root/part3_parallel_portfolios` | Designed four launch portfolios: boundary container, comparable-cluster incidence, repeated-prime-power partial layers, and central-binomial carry loss. It proved none of their terminal lemmas. |
| `/root/active_part3_triage` | Confirmed the target and verifier were still open and proposed UDL, FRT, TCI/reset-packing, boundary-overlap, and ballot continuations. |
| `/root/devbox_capacity` | Operational capacity check only; it found inference/transport, not local CPU or memory, to be the bottleneck. |

Its other child sessions concerned Part (i), Lean release work, harness
operation, or unrelated problem selection and are outside this mathematical
Part-(iii) audit.

## Continuation-root mathematical descendants

The following is an exact inventory of the 28 descendant session files whose
first `session_meta.parent_thread_id` is either the continuation root or its
`unequal_power_tuner` child. Every row's terminal assistant report was read,
not merely its filename.

| Agent path | Terminal mathematical disposition |
|---|---|
| `/root/derive_from_i_ii` | Derived exact annular two-base Lucas conditions. CRT can erase every member of any finite geometric exponent menu, so the construction does not close the target. |
| `/root/valuation_route` | Proved a higher-order Giuga-lock cofactor lower bound \(R>Y/|S|^{1/h}\). The bound remains compatible with the smooth sublevel cap. |
| `/root/dual_counteraudit` | Rechecked the Giuga controller tax, coherence sums, and capped example \(1722\). The natural subset sum becomes tautological and no common row follows. |
| `/root/complement_row` | Proved exact divisor/complement-row valuation identities and isolated the needed concentration inequality. It did not prove that concentration. |
| `/root/unequal_power_tuner` | Derived positive depth-two complete-homogeneous integer relations, including \(J+K^2=2CT\). No scalable smooth hostile family or contradiction resulted. |
| `/root/unequal_power_tuner/tail_descent_counter` | Reduced pair hostility to the exact all-digit system \(qt\preceq_p qM,\ pt\preceq_q pM\). The squarefree \(m=2\) packet statement stayed open. |
| `/root/unequal_power_tuner/smooth_cofactor_lock` | Proved \(Q^{L-H+1}<C^H\) for a depth-\(L\) unit lock. This excludes only locks substantially deeper than packet size. |
| `/root/unequal_power_tuner/depth2_crt_obstruction` | Proved \(C>\max\{y/\sqrt H,\,2y^2/(H(H+1))\}\). At target packet sizes that lower bound is still compatible with \(C\le X\). |
| `/root/small_base_bridge` | Proved a three-partition prefix-difference bound \((L_1+1)(L_2+1)(L_3+1)\le3R^2+3R+2\). It is quantitatively empty when the cofactor \(R\) is exponential. |
| `/root/sublevel_budget` | Gave the exact equivalence between bounded row capacity and endpoint-only intersections for every packet subset, plus the corresponding all-\(t\) digit system. No growing smooth bounded-capacity family was constructed. |
| `/root/digit_markov_bridge` | Audited a fixed-base digit-distribution theorem against the actual triple target. Moving bases, moduli, functions, and interval cutoffs prevent transfer. |
| `/root/cofactor_one_growth` | Proved exact packet-incidence and factorial-moment identities. Their first-moment bounds are automatically compatible with capacity one and yield no growth theorem. |
| `/root/smooth_divisor_residue` | Proved the complete-homogeneous common-residue integer and used it to eliminate the retained fixed-alphabet BG counterfamily gate. |
| `/root/intrinsic_support_entropy` | Proved the exact fringe identity \(T(A,p,R)=J\cup(R-J)\) and an infinite endpoint-only fixed-packet family. It lacks packet growth and smoothness, so it is not a target counterfamily. |
| `/root/new_lemma_audit` | Rechecked the new complete-homogeneous, unequal-depth, and triple-prefix statements; supplied the missing \(y>2\) side condition and weaker constants where needed. |
| `/root/hostility_template_extraction` | Proved a dense orientation lemma: endpoint-only triples force a positive fraction of bases to be directionally hostile in many triples. Per-base hostility still does not synchronize one row. |
| `/root/smoothness_to_template` | Extended the common-residue identity to a fixed polynomial digit template. No theorem forces such a shared template in actual hard instances. |
| `/root/capacity_moment_attack` | Built a scalable abstract reflection/divisibility support model with capacity one and all formal moment identities. It certifies that those identities alone cannot prove the target. |
| `/root/final_math_audit` | Rechecked the packet report and narrowed three overclaims: the spectral and valuation no-gos apply only to their displayed data, and a proposed equivalence is merely sufficient. |
| `/root/k6_code_audit` | Independently certified the six-prime capacity-two packet, all 20 endpoint-only triples, and the exact pair witness. This is finite only. |
| `/root/triple_cover_product` | Derived the exact triple valuation/product identity \(V_p(S)\). Hostility yields only an already-automatic linear lower bound. |
| `/root/lucas_polynomial_cover` | Derived the exact cyclotomic shift-annihilator and uncertainty product. The bound is too weak at the actual cofactor scale. |
| `/root/amplitude_coupling` | Re-audited the early corpus and found missing cover, exponent-spectrum, singleton, depth-envelope, adjacent-row, rational-cut, ballot, BG, and mixed-depth entries; these are now incorporated in the canonical map. |
| `/root/smooth_phase_rigidity` | Constructed primitive characters arbitrarily close to principal on a whole packet, killing generic conductor-based damping. |
| `/root/cover_dual_contradiction` | Identified endpoint-only fibres with conformally indecomposable nonnegative kernel atoms. Minkowski, Siegel, and generic Graver bounds do not supply the required conformal split. |
| `/root/pascal_gcd_compression` | Proved that every multirow Pascal gcd is exactly an lcm support cover and derived its weighted layer identity. Closure still requires a sublinear physical-row cover. |
| `/root/direct_constructor_reset` | Re-audited the middle corpus and recovered DFC/ODHEGC, actual-pattern determinants, necklace resets, AQG and primorial falsifiers, the quartet, hard-sequence mechanics, TPRI, and UTC; all are now in the canonical map. |
| `/root/probabilistic_row_attack` | Re-audited the late corpus and recovered minimal overweight divisors, prime-punctured descent, actual-row Carmichael aggregation, and first-failure phase capacity; all are now in the canonical map. |

All 28 descendant sessions have a terminal report. None claims or proves an
unconditional Part-(iii) result. The three audit descendants did find
omissions in an earlier draft of the map; that is why their recovered items
are named explicitly rather than summarized as “already covered.”

## Brave/ChatGPT Pro sessions

These six signed-in Brave threads are separate from the local Codex JSONL
tree:

| Thread ID | Route / terminal disposition |
|---|---|
| `6a67e243-31fc-8329-a50d-03c04504e99e` | Broad sparse-shadow/Matveev argument. Its terminal response explicitly leaves the global estimate open at large-box high-conductor phase cancellation. |
| `6a667fdb-a9f0-8329-a158-d2c7c9034b9d` | Sparse-cylinder and bounded-deletion cyclotomic pass. Its claim ledger ends with the requested sparse-forcing implication and Erdős 700(iii) open. |
| `6a668048-74b4-832e-839d-cdad03dc5e26` | Hostile weighted route. It proves the sparse upper-component bound but leaves the converse weighted implication `TB` open; its terminal box says `CLAIM_SOLVED: NO`. |
| `6a67e296-c66c-832d-aa8e-b5a11fdffa4f` | Pascal--Cartier route. It proves the rank-one Cartier hierarchy and split-exponent/fixed-depth countermodels, then stops at full-depth smooth diagonal rigidity. |
| `6a666ed5-1aec-8327-ac10-041ff0c7fb6b` | Fibre/common-prefix route. It proves the common elementary-symmetric coefficient sequence and prefix-remainder entropy identity, then leaves the simultaneous-remainder estimate `(21)` and signed digit-sensitive Fourier bound open. |
| `6a67e26a-ae70-8325-a446-e5db722aeba2` | Character route; one-base character energy does not control signed cross-base phase. |

The first five threads were reopened during the second pass and their complete
terminal claim ledgers were checked. The character thread's disposition is
retained from the prior signed-in browser audit because Brave was in active
user use during the final recheck. All six remain browser evidence rather than
self-contained repository proofs.

## Conclusion

This matrix closes the source-level gap between “all dev-box runs were
indexed” and “all local discussion was indexed.” It still cannot prove that
an unrecorded thought never occurred. The defensible statement is:

> Every persisted claim-bearing dev-box run, local Codex mathematical fork,
> and identified Brave Pro thread has a recorded disposition. None contains
> an unconditional proof or genuine target counterfamily for Part (iii).
