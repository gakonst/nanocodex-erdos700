# Erdős 700(iii): adversarial counterexample to the Carmichael route

Try to refute the actual sufficient theorem

```text
H_lambda: lambda(n) <= D(n)^3
```

where

```text
D_n(k)=n/gcd(n,binomial(n,k)),
D(n)=max_{2<=k<=n/2} D_n(k).
```

Do not settle for refuting an auxiliary cover lemma. A counterexample must
give an unbounded family with `lambda(n)>D(n)^3` and a proof controlling
`D_n(k)` for every admissible `k`.

Recover all worker reports from `math-1784935021-3712996`, especially reports
4, 5, 7, 10, 11, 14--17, and 19. Never inspect `events.jsonl`, snapshots,
credentials, or telemetry.

The exact full-component criterion is

```text
q_i=p_i^a_i | D_n(k)
iff
k=q_i*t and t <=_{p_i} n/q_i.
```

Use partial layers as well whenever `n` is not squarefree. An abstract support
system is not realizable evidence.

Prioritize three designed architectures:

1. squarefree four-prime families that make all six adaptive PC4 digit systems
   empty while forcing `lcm(p_i-1)>P^3`;
2. many-component families with pairwise-coprime private Carmichael atoms but
   provably small realizable Lucas supports;
3. prime-power mixtures inspired by the exact three-row and fractional-cover
   falsifiers, with a uniform all-index bound.

For every architecture:

- state the parametric family and prove it is unbounded;
- state the exact all-index upper bound on `D_n(k)`;
- test the smallest symbolic member and record an exact certificate;
- distinguish an open simultaneous-primality assumption from an
  unconditional family;
- kill the route immediately if only a finite example or abstract incidence
  pattern survives.

Launch independent constructor, all-index auditor, number-theory feasibility,
and blind-disproof workers. If no genuine family survives, prove a structural
theorem excluding the broadest attempted architecture and state exactly how
that theorem strengthens the positive global-cover route.

Do not claim Erdős 700(iii) is false merely because `H_lambda` is false:
`H_lambda` is sufficient, not known equivalent. A genuine Part (iii)
counterexample requires the stronger original fixed-`A` asymptotic negation.
